import React from 'react';
import ReactDOM from 'react-dom'; //renders to the DOM
import App from './components/App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
